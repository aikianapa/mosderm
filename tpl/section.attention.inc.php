<section id="attention" data-name="Блок с предупреждением (синий фон)">
    <!-- tpl/section.attention.inc.php -->
    <div class="container">
        <div class="attention__wrap">
            <h3>{{data.content.header}}</h3>
            <strong>{{data.content.subheader}}</strong>
            <p>{{data.content.text}}</p>
            <div class="attention__btn">
                <a href="{{data.content.btn_link}}" target="{{data.content.btn_target}}">{{data.content.btn_name}}</a>
            </div>
        </div>
    </div>
</section>